<!DOCTYPE html>
<html>
    <head>
        <!-- Required meta tags -->
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-giJF6kkoqNQ00vy+HMDP7azOuL0xtbfIcaT9wjKHr8RbDVddVHyTfAAsrekwKmP1" crossorigin="anonymous">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.3.0/font/bootstrap-icons.css">
        <!-- Affichage titre de page  -->
        <title>{% block title %}{% endblock %}</title>
    </head>
    <body>
      <!-- _________________________________________________________________  -->
      <!-- BARRE DE NAVIGUATION  -->
      <div class="container">
        <div class="row">
          <div class="col-md-2" >

            <!-- TITRE -->
            <h1 style="padding-top:10px;"><span style="color:red;">Pro</span><span style="color:silver;">Stage</span></h1>

          </div>
          <div class="col-md">
            <ul class="nav justify-content-end" style="padding-top:15px;">

              <!-- LIENS DU MENU  -->
              <li class="nav-item">
                <a class="nav-link" aria-current="page" href="{{ path('prostages_Accueil') }}" style="color: inherit;">  Accueil</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('prostages_Entreprises') }}" style="color: inherit;">  Liste des entreprises</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="{{ path('prostages_Formations') }}" style="color: inherit;">  Liste des formations</a>
              </li>
			  <li class="nav-item">
				<a class="nav-link" href="{{ path('prostages_AjoutEntreprise') }}" style="color: inherit;"> Ajouter une entreprise</a>
			  </li>
			  <li class="nav-item">
				<a class="nav-link" href="{{ path('prostages_AjoutStage') }}" style="color: inherit;">Ajouter un stage</a>
			  </li>

            </ul>
          </div>
        </div>
      </div>

      <!-- CONTENUE DE LA PAGE  -->

      <div class="container">
        <div class="row">
          <div class="col-md-12">
            <div class="d-flex justify-content-center">

            <!-- MESSAGE DE DESCRIPTION DE PROSTAGE  -->
            <p style="padding-top:50px;">Bienvenue sur <span style="color:red;">Pro</span><span style="color:silver;">Stage</span> la référence en termes de stages</p>

            </div>
          </div>
        </div>
      </div>
        <br/>
        <div class="container">
          <div class="row">
            <div class="col-md-12">
              <!-- CONTENU A AJOUTER  -->
              {% block body %}{% endblock %}
            </div>
          </div>
        </div>
        {% block javascripts %}{% endblock %}

        <!-- Inclusion de bootstrap -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta1/dist/js/bootstrap.bundle.min.js" integrity="sha384-ygbV9kiqUc6oa4msXn9868pTtWMgiQaeYH7/t7LECLbyPA2x65Kgf80OJFdroafW" crossorigin="anonymous"></script>
    </body>
</html>